// 1 export using export default
// export default add;

// 2 defining export default directly into our code
// export default (a, b) => {
//   return a + b;
// };

// 3 export multiple funcanality

// const add = (a, b) => {
//   return a + b;
// };

// const subtract = (a, b) => {
//   return a - b;
// };

// export default { add, subtract };

// 4 defining export const in code

export const add = (a, b) => {
  return a + b;
};

export const subtract = (a, b) => {
  return a - b;
};
